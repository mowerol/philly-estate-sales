# MapTiler API key used for the map style/tiles (react-map-gl + maplibre-gl).
# Restrict this key to your deployed domain in the MapTiler dashboard — Vite bakes
# it into the public JS bundle at build time, so it is not a secret at runtime.
VITE_MAPTILER_KEY=
